# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build vahan-cms`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t vahan-cms`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system vahan-cms && \
          adduser --system -G vahan-cms vahan-cms

COPY dist/apps/vahan-cms vahan-cms
RUN chown -R vahan-cms:vahan-cms .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix vahan-cms --omit=dev -f install

CMD [ "node", "vahan-cms" ]
